# Do this first separate before the others
# !pip install -q torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Pip install -r requirements.txt
# --- geospatial + general ---
geopandas
osmnx
shapely
mercantile
vt2geojson
numpy>=1.24,<2.1
pandas>=2.0,<2.3
scipy>=1.10,<1.14
matplotlib
tqdm
requests
python-dotenv
Pillow

# --- imaging ---
opencv-python-headless

# --- segmentation stack (this is the key) ---
segment-geospatial[samgeo3]==1.2.1

# --- critical compatibility pin (you already needed this) ---
transformers>=4.38,<5
